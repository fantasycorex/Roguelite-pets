import { TowerConfig } from '../types/combat';

export const TOWERS_DATA: Record<string, TowerConfig> = {
  nexus_core: {
    id: 'nexus_core',
    name: 'Nexus Core',
    maxHp: 100,
    armor: 0,
  },
};
